1. Download HQDM and MagmaCore from github
2. Install maven and jdk15 on host
    - `sudo apt install maven`
    - `./install_jdk.sh`
        - Requires:
            pcregrep
3. Run `./compile.sh` to compile HQDM and MagmaCore